import React, { Component } from "react";

export default class List extends Component {
  //The "render()" returns JSX
  render() {
    return (
      <div>
        <p>Welcome to the List component</p>
      </div>
    );
  }
}
